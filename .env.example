# OpenAI API key for AI-powered recommendations.
# Must be set and valid for gpt-4o-mini to generate roadmap, projects, and interview questions.
# If missing or invalid, the app uses a rule-based fallback (see README AI Disclosure).
OPENAI_API_KEY=
